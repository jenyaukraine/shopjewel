{{ header }}
<article class="legal-page"><div class="breadcrumbs">{% for breadcrumb in breadcrumbs %}<a href="{{ breadcrumb.href }}">{{ breadcrumb.text }}</a>{% if not loop.last %}<span>/</span>{% endif %}{% endfor %}</div><header><p class="eyebrow">6MOMENTS</p><h1>{{ heading_title }}</h1></header><div class="legal-copy">{{ description }}</div></article>
{{ footer }}
